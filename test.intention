// @Pattern:

@:println("Hello World!!! from An intention");
@:println();

// The parameter count
@:println("$Args.length: " + $Args?.length);

// The each parameter
foreach(Object $Arg : $Args)
	@:printf("$Args[%s]: %s\n", $Count$, $Args?[$Count$]);
@:println();

